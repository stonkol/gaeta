import React from 'react'
import { DoughType, PizzaType, Drink, ToppingType } from '../App';

type Pizza3DProps = {
  doughType: string;
  pizzaType: string | null;
  toppings: string[];
};

export default Pizza3D;
